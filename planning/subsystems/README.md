# Subsystem Planning Folders

This directory mirrors domain subsystem boundaries in `rustBotNetwork/app_core/src/subsystems`.

Use one folder per subsystem for:
1. architecture notes
2. ADRs
3. implementation backlog
4. test strategy

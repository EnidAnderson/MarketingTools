{
  "name": "webassets",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "dev": "live-server . --port=3000 --no-browser --no-open",
    "build": "rm -rf dist && mkdir dist && cp index.html main.js style.css dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "live-server": "^1.2.2",
    "chart.js": "^4.4.1"
  }
}
